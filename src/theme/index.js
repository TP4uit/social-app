import { colors } from './colors';
import { spacing } from './spacing';
import { typography } from './typography';

// Export the theme object
export const theme = {
  colors,
  spacing,
  typography,
};

// Also export the individual objects for direct imports
export { colors, spacing, typography };